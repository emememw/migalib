{
  "name": "migalib",
  "version": "0.0.10",
  "description": "MiGaLib - Minimal Game Library",
  "author": "markus-wi",
  "main": "./dist/migalib.js",
  "scripts": {
    "build": "node build.js",
    "example": "npm run build && cd ./example && rm -rf ./node_modules/migalib && npm install && npm start"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/markus-wi/migalib"
  },
  "bugs": {
    "url": "https://github.com/markus-wi/migalib/issues"
  },
  "keyword": [
    "game",
    "game library",
    "game framework"
  ],
  "devDependencies": {
    "browserify": "^13.0.1",
    "browserify-derequire": "^0.9.4",
    "fs.extra": ">= 1.3.2",
    "rimraf": ">= 2.3.2"
  }
}
